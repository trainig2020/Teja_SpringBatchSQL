package com.config;

/*
 * @Component public class EmployeeMapper implements RowMapper<Employee> {
 * 
 * @Override public Employee mapRow(ResultSet rs, int rowNum) throws
 * SQLException { Employee employee = new Employee();
 * employee.setEmpId(rs.getInt("empId"));
 * employee.setEmpName(rs.getString("empName"));
 * employee.setMobile(rs.getString("mobile")); return employee; }
 * 
 * }
 */
